name: ci-verificar-codigo

on:
  push:
    branches: ["main"]

jobs:
  verificar_codigo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: validar html
        run: echo prueba

  desplegar_pagina:
    runs-on: ubuntu-latest
    needs: verificar_codigo
    steps:
      - name: Desplegar
        run: echo "Desplegando pagina..."